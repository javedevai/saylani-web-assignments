/*
Task 20: Game Start
Ask the user to type "start" to begin. If they do, alert "Game started!"; 
else alert "Waiting for input..."
*/

let input = prompt("Type 'start' to begin:");
if (input === "start") {
    alert("Game started!");
} else {
    alert("Waiting for input...");
}
