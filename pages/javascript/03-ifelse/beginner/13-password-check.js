/*
Task 13: Password Check
Ask the user for a password. If it matches "12345", log "Correct password"; 
else log "Wrong password".
*/

let password = prompt("Password")
if (password == 12345) {
  alert("Correct password")
} else {
  alert("Wrong Password")
}